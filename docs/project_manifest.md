# Project manifest

Index of projects that contribute knowledge to this OS.

**Contract:** each project entry should eventually conform to `config/engineering_kernel/PROJECT_SCHEMA.yaml`.

No project instances are registered here yet. When adding one, create a YAML (or Markdown + YAML front matter) document that satisfies the schema fields, and link it from this file.
